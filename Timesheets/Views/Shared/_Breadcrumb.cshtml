@using Falcorp.Timesheets.Api

<div class="col-md-3 left_col">
<div class="left_col scroll-view">

    <div class="navbar nav_title" style="border: 0;">
        <a href="@Url.Action("Index","Home")" class="site_title"><i class="fa fa-calendar"></i> &nbsp; <span>Timesheets  <sup class="text-danger"> <small> &nbsp; @*@ApplicationInfo.Environment*@ </small> </sup></span></a>
    </div>

    <div class="clearfix"></div>
    <br />
    <div id="sidebar-menu" class="main_menu_side hidden-print main_menu">
        @if (Request.IsAuthenticated)
        {
         <!-- /sidebar menu -->
                <!-- menu profile quick info -->
            <div class="profile clearfix">
                <div class="profile_pic">
                    <img src="@Url.Content(Resources.DefaultProfilePhoto)" alt="..." class="img-circle profile_img">
                </div>
                <div class="profile_info">
                    <h2>@User.Identity.GetPrimaryClient() </h2>
                    <span><small>Client Company</small></span>
                </div>
            </div>
                <!-- /menu profile quick info -->
                    <div class="menu_section">
                        <ul class="nav side-menu">
                            <li>
                                <a><i class="fa fa-table"></i> @Session["CurrentBill"]<span class="fa fa-chevron-down"></span></a>
                                <ul class="nav child_menu">
                                    <li><a href="@Url.Action("Index", "Home")">Dashboard</a></li>
                                    <li>
                                        <a>Timesheet &nbsp;<span class="fa fa-chevron-down"></span></a>
                                        <ul class="nav child_menu">
                                            <li class="sub_menu">
                                                <a href="@Url.Action("Index", "EmployeeTimes")">Capture</a>
                                            </li>
                                        </ul>
                                    </li>
                                </ul>
                            </li>
                        </ul>
                    </div>
        }
    </div> <!-- sidebar menu -->
    </div>
</div>